     
	    Batch8 notes :-
		git clone <httpd link>   ------> clone the project code copy from remote to local
		enter
		cd <name>
		git status --- need to check the status of file
		git add --all ----file need to be add which ever we modified/added
		git status
		git push --- push the changes from local to remote 
		
		ssh-keygen -t ed25519 -C "your_email@example.com"


How to switch my own branch?

git checkout <branch name>
git checkout feature/2026.06.12



GIT Command :-


git clone

cd

git status

git add --all

after added the files we need to verify the status of the files

git status


git clone ----> clone the project code from remote to local

git clone https://github.com/srinfotechbatch8/SRINFOTECHBatch8.git

enter

cd SRINFOTECHBatch8

git status ---> check the status of files

git add --all --->add the fiels which ever you modified/added git status ---> check the status of files

git push --->push the changes from local to remote

git clone cd git status git add --all git commit -m "updated readme file & added new batch8 files" git push

git commit -m "message"

git push ---> push the changes from local machine to remote




$ git checkout feature/2026.06.28
error: pathspec 'feature/2026.06.28' did not match any file(s) known to git


shilp@Shilpa MINGW64 /d/shilpapratice/Analyzer (main)
$ git fetch origin
From github.com:shilpapatanaik/Analyzer
 * [new branch]      feature/2026.06.28 -> origin/feature/2026.06.28


 git fetch origin ---> if we are getting error to switching one branch to another we have to do like this
 
  git checkout feature/2026.06.28 (it will work)
